<div class="col-md-4">
	<%= form_for @item do |form| %>
		<div class="form-group">
			<%= form.label :Usuário %>
			<%=form.collection_select(:user_id, User.all, :name, :name) %>
		</div>
		<div class="form-group">
			<%= form.label :Status %>
			<%=form.collection_select(:status, OrderStatus.all, :name, :name) %>
		</div>
		<%= form.submit class: "btn btn-primary" %>
	<% end %>
</div>

<br>
<br>

<%= render 'templates/back_button' %>