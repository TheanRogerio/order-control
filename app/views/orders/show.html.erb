<h1>Pedido #<%= @order.id %></h1>

<p>
  <strong>Total:</strong>
  <%= @order.total %>
</p>

<p>
  <strong>Status:</strong>
  <%= @order.status %>
</p>

<%= link_to 'Editar', edit_order_path(@order), class: 'btn btn-primary' %>
<%= render 'back_button' %>